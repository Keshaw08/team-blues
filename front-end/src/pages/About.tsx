import AboutUs from "@/components/AboutUs";
import Header from "@/components/Header";

type Props = {};

const About = ({}: Props) => {
  return (
    <div>
      <Header />
      <AboutUs />
    </div>
  );
};
export default About;
